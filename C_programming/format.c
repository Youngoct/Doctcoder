#include <stdio.h>

/**
 * main - print the format
 *
 * Return: 0 as success
 */

int main(void)
{
	float j = 40.912;
	char m = 'B';
	int w = 46;

	printf("value of j = %f\n", j);
	printf("value of m = %C\n", m);
	printf("value of w = %i\n", w);
	return (0);
}

